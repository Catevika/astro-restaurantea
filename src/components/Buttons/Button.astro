---
interface Props {
  classes?: string[] | string;
  type?: "submit" | "button";
  text: string;
}

const { classes, type = "submit", text, ...rest } = Astro.props;
---

<button {type} class:list={[classes]} title={text} {...rest}>{text}</button>
